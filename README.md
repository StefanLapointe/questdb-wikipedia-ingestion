# questdb-wikipedia-ingestion

Currently just a simple Python script to ingest data from Wikipedia's recentchange EventStream into QuestDB.

You must have a QuestDB database available at 127.0.0.1:9000.

Launch the script using `uv run main.py`.
